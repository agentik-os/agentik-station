# Acceptance

Run both workflows with synthetic fixtures, validate every output, reject invalid schemas/cycles/horizontal slices and missing appetite. Confirm all three native profiles and the generated role map. Then the owning human accepts a real journey-to-release artifact through the exact Director. No live provider/chat acceptance is inferred from package tests.
