# Synthetic walking skeleton

The input recall example is retained as a fake-record/fake-send exercise. Never interpret it as medical advice, consent or permission to contact patients. Four typed artifacts and both workflow requests are executable validator fixtures. They contain no secrets, network endpoints or real account identity.
