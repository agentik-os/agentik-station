# Fleet Operator OS

Operate Host registry, Zone placement, remote Doctor, desired-state application, release rings, backups and explicit remote operations without copying production secrets into Control.
