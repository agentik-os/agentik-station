# Discord Bootstrap OS

Provision and reconcile roles, categories, channels, permissions, commands and OS/team bindings. Administrator is bootstrap-only when narrower permissions are insufficient. The human server owner creates/authorizes the application, controls its token, removes temporary elevation and then Station verifies runtime permissions before `OPERATIONAL`.
