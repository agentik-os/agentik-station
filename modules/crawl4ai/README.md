# crawl4ai

Crawl4AI LLM crawler is declared for optional user install; tool allowlist and network egress policy pending.

Next: Install crawl4ai for agk-station, allowlist in Hermes tools, run one crawl with evidence receipt.
