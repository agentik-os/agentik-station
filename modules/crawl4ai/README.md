# crawl4ai

Crawl4AI 0.9.3 installs as an isolated `uv` tool and must pass `crawl4ai-doctor`; Hermes tool allowlisting and network egress policy remain pending.

Next: Install crawl4ai for agk-station, allowlist in Hermes tools, run one crawl with evidence receipt.
