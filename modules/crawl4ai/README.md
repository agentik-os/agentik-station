# crawl4ai

Crawl4AI 0.9.3 installs by default in a versioned read-only Python runtime. Hermes exposes `station_crawl4ai` as the explicit no-LLM, no-JavaScript Markdown fallback. `station deps web-check` verifies imports, pins and Chromium launch. See [resource contract](../../resources/crawl4ai/README.md) for paths, limits and activation.

Next: Install crawl4ai for agk-station, run a public test URL through Hermes and record the extraction evidence readback.
