# fleet-control

Safe typed remote bootstrap with strict SSH checking, immutable release archive, operation IDs and receipts is implemented; persistent drift/reconcile agent and remote rollback acceptance are pending.
