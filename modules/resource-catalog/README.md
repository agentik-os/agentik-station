# Resource catalog

Versioned resources and a queryable preferred-stack plan for Hermes and coding executors. The module is `INSTALLABLE`; a Project becomes configured only after dependency installation, provider enrollment and readback in that Project.
