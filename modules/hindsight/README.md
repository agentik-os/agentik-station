# hindsight

Hindsight client 0.9.2 installs into its own Station venv. Hermes provides the native runtime integration through `hermes memory setup`; learning-store isolation remains pending.

Next: Install per upstream README, bind to one Hermes profile, verify recall without exporting secrets to Control.
