# honcho

Honcho SDK 2.4.0 installs into its own Station venv; server/API enrollment and Zone-local memory policy remain pending.

Next: Install package, configure Zone-scoped credentials/store, then Doctor a memory round-trip without cross-Zone leakage.
