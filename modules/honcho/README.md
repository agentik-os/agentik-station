# honcho

Honcho agent-memory dependency is declared for optional pip install under agk-station; Zone-local memory policy pending.

Next: Install package, configure Zone-scoped credentials/store, then Doctor a memory round-trip without cross-Zone leakage.
