# Station History

This directory preserves historical Station generations for provenance and archaeology.

**Nothing under `docs/history/` is canonical runtime guidance.** Agents must use the current root documents and current `docs/` sections first. Historical snapshots may contain superseded terminology, paths, policies, or implementation choices.

- `v9/` preserves the last pre-Universal-Zone generation and its complete snapshot.
- current architecture: [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md)
