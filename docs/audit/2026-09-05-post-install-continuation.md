# Post-installation continuation

## Scope

Continue from reviewed commit `ae7e0c7` / immutable Host release 11.33. Preserve
the verified Stepper/Builder teams, existing accounts, services and client scope.
Correct confirmed post-install guidance and readiness contradictions; do not
invent missing accounts, bypass Ponytail's scanner or activate server workloads.

## Plan First

```text
clean main + live release/CI readback
├── Ponytail native gate + official upstream review → correct stale claims
├── onboarding scope/action inspection → reproduce before selecting any repair
└── retained acceptance evidence → close the 11.33 audit accurately
    ↓
targeted regression → repository Doctor → reviewed version/publication
    ↓
report remaining external/account gates with exact next actions
```

Ponytail research and onboarding inspection are independent read-only branches.
The main agent owns global metadata, module catalog and acceptance evidence.
Any delegated correction has exact file ownership and independent main-agent
review. No new gateway/account/service activation is part of this continuation.

## Acceptance

- Source/module guidance must not claim the blocked plugin is installed or
  recommend an unpinned direct install as a repair.
- Existing guard and independent dependency continuation remain unchanged.
- A provider or Director setup command must retain its explicit owning scope.
- Published evidence distinguishes software tests, scoped native validators and
  unexercised account/live-mission acceptance.
- Tests, Doctor and final source integrity are required for any changed release.

## Reproduced findings and selected corrections

- Ponytail's module/catalog and engineering guidance falsely said installed or
  installable and recommended retrying the blocked tree. The required integration
  is now `SCAFFOLDED` / `NOT_INSTALLED` with the actual upstream-review next action.
  The native rejection and independent software-install continuation are unchanged.
- The actual runtime reader downgraded changed verified configuration to
  `CONFIGURED` / stale, but gateway activation only rejected `DEGRADED`.
  OS-scoped install/start/restart now require current full-team verification and
  return an exact repair command. Zone default and repair/observation are unchanged.
- Composio already has root-owned shared software, but the selector could prefer
  the private operator PATH/fallback before invoking it as a Zone. A real Zone
  executable-access check passed for the shared path and failed for the private
  one. Repair selects the reviewed public export; no account was authenticated.
- Instance onboarding now distinguishes mapped persistent-profile provider tests
  from transient children. Historical 11.33 native/CI evidence is closed separately.
- Pinned Composio 0.4.0 requires developer subcommands, an explicit principal and
  matching project/workdir context. The old facade had neither valid grammar nor
  a trusted developer binding and could select a consumer identity. Plan now
  emits non-executable templates; link/verify refuse before native/account calls.
  The missing binding implementation is explicit, not disguised as missing keys.
  The pinned-source contract and atlas/setup guidance describe this limitation.

## Pre-publication validation

- Complete final Station/Factory suite: 2,256 passed, 23 Linux-only skips on macOS.
- npm suite: 263 passed. Packed-consumer offline installation, launcher, payload
  and read-only plan smoke passed for `@agentik-os/station@11.34.0`.
- Repository Doctor: 97 checks, no issues or warnings.
- Independent reviews covered the gateway verification gate, Ponytail claims,
  pinned Composio source/selector and user-facing enrollment guidance.
- Final documentation/metadata contracts: 255 passed, two Linux-only skips;
  deterministic metadata and diff checks passed. Published-commit CI and native
  kernel readback are subsequent gates, not inferred from these results.

Native acceptance uses the exact retained Host spec and compares 17 private-file
fingerprints plus four tracked service activation states. It does not reinstall
or migrate the existing Stepper 0.2.0, Builder 11.14 or Librarian 3.0.1 teams.
No new account enrollment, service activation or Ponytail scan bypass is part
of this acceptance; npm registry publication remains separate.
