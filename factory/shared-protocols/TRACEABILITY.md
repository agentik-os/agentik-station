# Traceability

Release traceability chain:

`mission requirement → research input(s) → design decision → contract component → implementation → test/eval → evidence → release`

No critical feature should terminate at “agent prompt says so” without an eval or verification mechanism.
