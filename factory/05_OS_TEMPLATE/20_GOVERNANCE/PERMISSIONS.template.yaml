scopes:
  organization: explicit
  project: explicit
  os: explicit
  mission: explicit
credentials:
  package_policy: references_only
  inheritance: deny_by_default
memory:
  cross_organization: deny
  cross_client: deny
  write_authority: explicit
approvals:
  destructive_actions: owner_or_policy
  external_side_effects: capability_policy
