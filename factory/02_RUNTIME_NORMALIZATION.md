# Runtime Normalization Rules

When an imported OS/factory pack conflicts with current Station canonical architecture:

```text
Station Constitution
> Locked AGK OS Contract
> current Hermes native semantics
> active OS contract
> archival/source-pack prose
```

Archival packs are evidence/source inputs, not automatically executable desired state.

Builder Doctor must surface conflicts rather than silently choosing one.
