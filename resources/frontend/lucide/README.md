# Lucide resource

`lucide-react` is the default icon dependency for Station web-product Projects. Install the exact catalog pin in the Project; import only used icons and keep icons semantic and accessible. Other icon systems remain possible when the Project contract names the reason and version.
