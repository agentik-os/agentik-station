# shadcn/ui resource

The pinned `shadcn` CLI is installed for the Station operator by `station deps toolchain-install`. Components are then initialized and added inside a Project repository, so their source remains reviewable and owned by that repository. Never treat a global CLI installation as proof that a Project is configured.
